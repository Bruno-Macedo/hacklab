#### Wifi kali ####
iwlist wlan0 scan  = find SSID
nmcli d wifi connect "Ireni Macedo" = connect 

#### bloodhound ####
neo4j:admin

#### Windows ####
https://developer.microsoft.com/en-us/windows/downloads/virtual-machines/
IEUser:Passw0rd!
Get-ExecutionPolicy -List
Set-ExecutionPolicy Unrestricted -Scope Process
Install-Module PSWindowsUpdate
Install-WindowsUpdate -AcceptAll

### SSH VPS ###
ssh-keygen -t rsa -b 4096 -f vps-ssh
ssh root@<vps-ip-address>
adduser cry0l1t3
usermod -aG sudo cry0l1t3
su - cry0l1t3
Add pub key to vps
mkdir ~/.ssh
echo '<vps-ssh.pub>' > ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
ssh cry0l1t3@<vps-ip-address> -i vps-ssh

## Aliases
# Create file ~/.bash_aliases 
alias up="sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y"
##alias start="expressvpn disconnect ; code workspace ; vpn -ad ; tmux new -s htb"
##alias start="code workspace ; vpn -ad ; tmux new -s htb"
alias rustscan="docker run -it --rm --name rustscan rustscan/rustscan:2.1.1"


# Add lines to .bashrc
    if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
    fi
    source ~/.bash_aliases

# Vs code extensions
Dev Containers
docker
Markdown All in One
Markdown Preview Mermaid
Markdown Tables
Pylance
Python
Python Debugger


box='function boxfolder(){ cp -pr /home/kaliwork/workspace/hacklab/room_template /home/kaliwork/workspace/hacklab/$1; mv /home/kaliwork/workspace/hacklab/$1/room_Template.md /home/kaliwork/workspace/hacklab/$1/$1.md; }; boxfolder'